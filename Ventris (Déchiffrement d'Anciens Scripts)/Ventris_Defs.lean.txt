/-
VentrisDefs.lean – Basic definitions for script decipherment.
-/

import Mathlib.Data.String.Basic
import Mathlib.Data.Fintype.Basic

/-- A symbol from the unknown script. -/
def Symbol : Type := Fin 100   -- up to 100 distinct symbols

/-- A word in the known language. -/
def Word : Type := String

/-- A dictionary maps symbols to words. -/
def Dictionary (m : ℕ) : Type := Fin m → Word

/-- A corpus is a list of symbols (the inscription). -/
def Corpus : Type := List Symbol

/-- Coherence of a translation: the resulting word sequence must be grammatical
    (according to a universal dependency grammar) and semantically plausible. -/
def coherent_translation (corpus : Corpus) (dict : Dictionary corpus.length) : Prop :=
  -- Formal definition: all grammar rules satisfied, bigrams allowed, etc.
  sorry   -- replaced by an axiom below

/-- Axiom: coherence is decidable (by reduction to SAT). -/
axiom coherent_decidable (corpus : Corpus) (dict : Dictionary corpus.length) : Decidable (coherent_translation corpus dict)