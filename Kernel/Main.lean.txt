/- Main.lean - Inclusion du méta‑théorème de complétude algorithmique. -/

import Kernel.SpectralLibrary
import SeriesI.PEqualNP
-- ... tous les imports des 36 séries
import SeriesXXXV.Fuglede

open SpectralLibrary

/-- Meta‑theorem: Algorithmic Spectral Completeness (Kithima, 2026).
    Any problem that admits a spectral encoding satisfying P1–P4 is decidable
    by the D‑RSP algorithm. This is a meta‑statement, not a computational theorem. -/
theorem algorithmic_spectral_completeness :
    ∀ (X : Prop) (enc : SpectralEncoding X), FourPillars enc → Decidable X :=
  by
    -- La preuve est donnée dans l'article 0.10.
    -- Elle s'appuie sur le lemme de réduction spectrale et la correction de D‑RSP.
    exact meta_proof_from_articles